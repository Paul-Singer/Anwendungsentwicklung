services:
  ghcn-backend:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      CORS_ORIGIN: "*"
      DATA_DIR: /data
      SERVE_STATIC: "1"
    volumes:
      - ghcn-data:/data

volumes:
  ghcn-data:
